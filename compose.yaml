services:
  realm:
    build: .
    ports:
      - "8080:8080"
    environment:
      SPRING_H2_CONSOLE_ENABLED: "false"
      CORS_ALLOWED_ORIGINS: "http://localhost:8080"
    volumes:
      - realm-data:/app/data
    restart: unless-stopped

volumes:
  realm-data:
