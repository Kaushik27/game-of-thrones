import { useState } from "react";
import { getEpisodes, getEvents } from "../api";
import PageState from "../components/PageState";
import SeasonFilter from "../components/SeasonFilter";
import { useResource } from "../hooks/useResource";

export default function StoriesPage(){const [season,setSeason]=useState<number>();const episodes=useResource(s=>getEpisodes(season,s),[season]);const events=useResource(s=>getEvents(season,s),[season]);return <main className="page"><div className="page-hero"><p className="eyebrow">EPISODE + EVENT SERVICES</p><h1>The story, season by season</h1><p>Filtering changes the URL parameters sent to Spring Boot. The backend queries H2 and returns only the requested season.</p></div><SeasonFilter value={season} onChange={setSeason}/><PageState loading={episodes.loading||events.loading} error={episodes.error||events.error}/><div className="story-layout"><section><div className="section-heading"><div><p className="eyebrow">{episodes.data?.itemsCount||0} EPISODES</p><h2>Episode catalogue</h2></div></div><div className="episode-list">{episodes.data?.items.map(episode=><article key={episode.id}><div className="episode-number"><strong>S{episode.season}</strong><span>E{episode.episode}</span></div><div><p className="eyebrow">{episode.airDate} · {episode.runtimeMinutes} MINUTES</p><h3>{episode.title}</h3><p>{episode.summary}</p><div className="tags">{episode.themes.map(theme=><span key={theme}>{theme}</span>)}</div><small>Directed by {episode.director}</small></div></article>)}</div></section><aside><div className="section-heading"><div><p className="eyebrow">{events.data?.itemsCount||0} TURNING POINTS</p><h2>Event timeline</h2></div></div><div className="event-timeline">{events.data?.items.map(event=><article key={event.id}><span className={`event-type ${event.type}`}>{event.type}</span><strong>{event.title}</strong><p>{event.summary}</p></article>)}</div></aside></div></main>}
